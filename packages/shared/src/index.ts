// Utilitaires partagés entre apps/api, apps/web et apps/mobile (à venir) :
// formatage de dates/prix DZD, helpers de validation de numéro algérien,
// constantes communes (rayon de recherche par défaut, etc.).
//
// Reste volontairement vide au MVP pour éviter toute duplication prématurée
// — à peupler au fur et à mesure que de la logique se répète entre apps.
export const DEFAULT_SEARCH_RADIUS_KM = 5;
